interface:
  display_name: "Curate"
  short_description: "Evolve style memory and contracts"
  default_prompt: "Use $curate to explore or revise the style baseline. Generate reference candidates with GPT Image 2 or return a blocked manifest, then stop at the next user selection or approval gate."
